CREATE TABLE IF NOT EXISTS subforums (
	subforum_id tinyint unsigned primary key auto_increment,
	subforum_name varchar(100),
	subforum_order tinyint unsigned
);

CREATE TABLE IF NOT EXISTS boards (
	board_id tinyint unsigned primary key auto_increment,
	subforum_id tinyint unsigned,
	board_name varchar(100),
	board_description text,
	board_order tinyint unsigned,
	last_post_id mediumint unsigned NULL,
	num_threads mediumint unsigned default '0',
	num_posts mediumint unsigned default '0',
	auth_view tinyint unsigned default '5', //What user level can view this forum, default everyone
	anonymous_posting enum('true','false') default 'false'
);

CREATE TABLE IF NOT EXISTS threads (
	thread_id mediumint unsigned primary key auto_increment,
	board_id tinyint unsigned,
	thread_name varchar(100),
	poster_id mediumint unsigned,
	thread_text text,
	time_posted datetime,
	time_updated datetime,
	num_replies smallint unsigned default '0',
	locked enum('true','false') default 'false',
	sticky enum('true','false') default 'false',
	deleted enum('true','false') default 'false'
);

CREATE TABLE IF NOT EXISTS posts (
	post_id mediumint unsigned primary key auto_increment,
	thread_id mediumint unsigned,
	board_id tinyint unsigned,
	poster_id mediumint unsigned,
	poster_name varchar(100),
	post_time datetime,
	edit_time datetime
);

CREATE TABLE IF NOT EXISTS post_text (
	post_id mediumint unsigned,
	post_text text
);

CREATE TABLE IF NOT EXISTS squffies (
	squffy_id int unsigned primary key auto_increment,
	squffy_owner mediumint unsigned,
	squffy_name varchar(100),
	squffy_gender enum('M', 'F') default 'M',
	squffy_birthday datetime,
	squffy_species tinyint unsigned,
	squffy_degree tinyint unsigned,
	degree_type enum('Apprentice', 'Master'),
	hunger tinyint unsigned default '0',
	health tinyint unsigned default '100',
	energy tinyint unsigned default '100',
	happiness tinyint unsigned default '100',
	luck tinyint unsigned default '0',
	c1 tinyint unsigned default '0',
	c2 tinyint unsigned default '0',
	c3 tinyint unsigned default '0',
	c4 tinyint unsigned default '0',
	c5 tinyint unsigned default '0',
	c6 tinyint unsigned default '0',
	c7 tinyint unsigned default '0',
	c8 tinyint unsigned default '0',
	base_color char(6),
	eye_color char(6),
	foot_color char(6),
	is_custom enum('true', 'false') default 'false', //if true, affects age
	is_pregnant enum('true', 'false') default 'false',
	is_in_market enum('true', 'false') default 'false',
	is_hireable enum('true', 'false') default 'false',
	is_working enum('true', 'false') default 'false',
	is_breedable enum('true', 'false') default 'false',
	strength1_id tinyint unsigned,
	strength2_id tinyint unsigned,
	weakness1_id tinyint unsigned,
	weakness2_id tinyint unsigned,
	mate_id int unsigned,
	breeding_rights mediumint unsigned,
	rights_revert datetime,
	num_items tinyint unsigned default '0'
	//Breed price
	//Hire price
);	

CREATE TABLE IF NOT EXISTS squffy_items (
	squffy_id int unsigned,
	item_id smallint unsigned,
	item_name varchar(100),
	item_order tinyint unsigned default '0'
);

CREATE TABLE IF NOT EXISTS squffy_family (
	squffy_id int unsigned,
	mother_id int unsigned,
	father_id int unsigned,
	mother_mother_id int unsigned,
	mother_father_id int unsigned,
	father_mother_id int unsigned,
	father_father_id int unsigned
);

CREATE TABLE IF NOT EXISTS squffy_appearance (
	squffy_id mediumint unsigned,
	trait_id tinyint unsigned,
	trait_square enum('S','C','D') default 'C',
	trait_color varchar(6),
	trait_order tinyint unsigned default '1'
);

CREATE TABLE IF NOT EXISTS appearance_traits (
	trait_id tinyint unsigned primary key auto_increment,
	trait_name varchar(255),
	trait_title varchar(255),
	trait_type enum('1','2') default '1',
	design_activated enum('true','false') default 'false',
	activated enum('true','false') default 'false'
);

CREATE TABLE IF NOT EXISTS personality_traits (
	trait_id tinyint unsigned primary key auto_increment,
	trait_good_name varchar(100),
	trait_bad_name varchar(100),
	trait_good_desc text,
	trait_bad_desc text
);

CREATE TABLE IF NOT EXISTS species (
	species_id tinyint unsigned auto_increment,
	species_name varchar(255),
	design_activated enum('true','false') default 'false',
	activated enum('true','false') default 'false',
	primary key (species_id)
);

CREATE TABLE IF NOT EXISTS cache_changed (
	user_id mediumint unsigned
);

CREATE TABLE IF NOT EXISTS degrees (
	degree_id tinyint unsigned primary key auto_increment,
	degree_name varchar(50)
);

CREATE TABLE IF NOT EXISTS layouts (
	layout_id tinyint unsigned primary key auto_increment,
	layout_name varchar(50),
	is_default enum('true','false') default 'false',
	background_color char(6),
	level_id tinyint unsigned
);

CREATE TABLE IF NOT EXISTS users (
	user_id mediumint unsigned primary key auto_increment,
	username varchar(50),
	email varchar(50),
	level_id tinyint unsigned,
	date_last_seen datetime,
	default_layout_id tinyint unsigned
);

CREATE TABLE IF NOT EXISTS inventory (
	user_id mediumint unsigned,
	ITEMNAME smallint unsigned ...
);

CREATE TABLE IF NOT EXISTS pantry (
	user_id mediumint unsigned,
	FOODNAME smallint unsigned ...
);

CREATE TABLE IF NOT EXISTS messages (
	message_id int unsigned primary key auto_increment,
	to_id mediumint unsigned,
	from_id mediumint unsigned,
	subject varchar(127),
	message text,
	time_sent datetime,
	is_read enum('true','false') default 'false',
	in_inbox enum('true','false') default 'true',
	in_outbox enum('true','false') default 'true'
);

CREATE TABLE IF NOT EXISTS news (
	news_id mediumint unsigned primary key auto_increment,
	subject varchar(127),
	content text,
	date_posted datetime,
	poster_id mediumint unsigned
);

CREATE TABLE IF NOT EXISTS news_comments (
	comment_id mediumint unsigned primary key auto_increment,
	news_id mediumint unsigned,
	poster_id mediumint unsigned,
	content text,
	date_posted datetime
);

CREATE TABLE IF NOT EXISTS weather (
	day enum('Today','Tomorrow'),
	temperature tinyint,
	type enum('Sunny', 'Partly Cloudy', 'Mostly Cloudy', 'Overcast', 'Light Rain', 'Heavy Rain', 'Snow')
);

CREATE TABLE IF NOT EXISTS badges (
	badge_id tinyint unsigned primary key auto_increment,
	badge_name varchar(100),
	badge_description text,
	num_awarded mediumint unsigned default '0'
);

CREATE TABLE IF NOT EXISTS user_badges (
	user_id mediumint unsigned,
	badge_id tinyint unsigned
);

CREATE TABLE IF NOT EXISTS user_login (
	login_name varchar(50) primary key,
	user_id mediumint unsigned,
	hash varchar(40),
	salt varchar(30)
);

CREATE TABLE IF NOT EXISTS news (
	news_id mediumint unsigned primary key auto_increment,
	user_id mediumint unsigned,
	time_posted datetime,
	subject varchar(100),
	content text,
	content_type tinyint unsigned,
	deleted enum('true', 'false') default 'false'
);

CREATE TABLE IF NOT EXISTS news_categories (
	type_id tinyint unsigned,
	type_name varchar(100)
);

CREATE TABLE IF NOT EXISTS news_comments (
	comment_id mediumint unsigned primary key auto_increment,
	news_id mediumint unsigned,
	user_id mediumint unsigned,
	time_posted datetime,
	content text,
	deleted enum('true', 'false') default 'false'
);

CREATE TABLE IF NOT EXISTS pregnancies (
	pregnancy_id int unsigned primary key auto_increment,
	mother_id int unsigned,
	father_id int unsigned,
	user_id mediumint unsigned,
	date_birth datetime
);

CREATE TABLE IF NOT EXISTS vacations (
	user_id mediumint unsigned,
	date_return datetime,
	old_level tinyint unsigned
);

CREATE TABLE IF NOT EXISTS referrals (
	referral_id int unsigned primary key auto_increment,
	refer_id mediumint unsigned,
	email varchar(255),
	date_sent datetime
);

CREATE TABLE IF NOT EXISTS accepted_referrals (
	accepted_id int unsigned primary key auto_increment,
	referral_id int unsigned,
	accept_id mediumint unsigned,
	date_joined datetime
);

CREATE TABLE IF NOT EXISTS newbie_packs (
	pack_id mediumint unsigned primary key auto_increment,
	user_id mediumint unsigned,
	species enum('Unchosen', 'Tree', 'Ground') default 'Unchosen',
	items_claimed enum('true', 'false') default 'false',
	squffy_made enum('true', 'false') default 'false'
);

CREATE TABLE IF NOT EXISTS degree_progress (
	squffy_id int unsigned,
	teacher_id int unsigned,
	date_finished datetime
);

CREATE TABLE IF NOT EXISTS jobs_doctor (
	patient_id int unsigned,
	doctor_id int unsigned,
	date_finished datetime
);

CREATE TABLE IF NOT EXISTS jobs_farming (
	farmer_id int unsigned,
	farm_id int unsigned,
	chore_type enum('Plow', 'Fertilize', 'Plant', 'Water', 'Weed', 'Harvest', 'Clear'),
	date_finished datetime
);

CREATE TABLE IF NOT EXISTS farms (
	farm_id int unsigned primary key auto_increment,
	farm_name varchar(50),
	user_id mediumint unsigned,
	food_id smallint unsigned,
	cur_state enum('Empty', 'Plowed', 'Fertilized', 'Planted', 'Grown', 'Dead'),
	dryness tinyint unsigned default '0',
	weeds tinyint unsigned default '0'
);

//Tables for all jobs that need them
//Marketplace
//Tournaments
//Notifications
//Payments
//Random events
//Bug reports: categories or enum?

CREATE TABLE IF NOT EXISTS user_activation (
	user_id mediumint unsigned,
	activate varchar(30)
);

CREATE TABLE IF NOT EXISTS design_traits (
	design_id int unsigned,
	trait_id tinyint unsigned,
	trait_color varchar(6),
	trait_order tinyint unsigned default '1'
);